# infrastructure

Terraform for env resources (`dev` first, later `prd`). Additive: create names already in [docs/environments.md](../docs/environments.md). Do not replace Compose or host `uv`.

**Order:** [docs/backlog.md](../docs/backlog.md) — local apply = item **5**; VPS / Actions / `prd` = item **10**.

Local Docker Compose is not replaced by this folder.
