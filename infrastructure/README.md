# infrastructure

Terraform (dev/prd) for env promotion. Phase 2. Additive: create names already in [docs/environments.md](../docs/environments.md). Do not replace Compose or host `uv`.

Local Docker Compose is not replaced by this folder.
