version: 2

sources:
  - name: dlt_smoke_raw
    description: >
      Bronze rows from tests/integration/dlt_lakehouse_smoke.py.
      Iceberg: nexus_{env}.raw_dlt_smoke.smoke (staging SQL uses iceberg_catalog() macro).
    schema: raw_dlt_smoke
    tables:
      - name: smoke
        columns:
          - name: id
            data_tests:
              - not_null
          - name: run_id
            data_tests:
              - not_null
          - name: _dlt_id
            data_tests:
              - not_null
              - unique
