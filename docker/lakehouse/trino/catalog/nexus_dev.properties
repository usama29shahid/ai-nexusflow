# Trino Iceberg catalog → Polaris REST (catalog nexus_dev).
# Local Docker: read Iceberg files from MinIO on the Compose network.
# Host dlt/pyiceberg uses localhost:9002; containers must use minio:9000
# (Polaris-vended endpoints are the host port and fail inside Trino).
connector.name=iceberg
iceberg.catalog.type=rest
iceberg.rest-catalog.uri=http://polaris:8181/api/catalog
iceberg.rest-catalog.security=OAUTH2
iceberg.rest-catalog.oauth2.credential=${ENV:POLARIS_CLIENT_ID}:${ENV:POLARIS_CLIENT_SECRET}
iceberg.rest-catalog.oauth2.scope=PRINCIPAL_ROLE:ALL
iceberg.rest-catalog.warehouse=nexus_dev
iceberg.rest-catalog.vended-credentials-enabled=false

fs.native-s3.enabled=true
s3.endpoint=http://minio:9000
s3.path-style-access=true
s3.region=us-east-1
s3.aws-access-key=${ENV:MINIO_ROOT_USER}
s3.aws-secret-key=${ENV:MINIO_ROOT_PASSWORD}
